{% extends "../../layouts/base.twig" %}

    {% block main_content %}
        <h2>About {{variant.title}}</h2>
        <ul style="list-style: none;">
            <li>
                <label>ID: </label> {{variant.id}}
            </li>
            <li>
                <label>URL Slug:</label> {{variant.slug}}
            </li>
            <li>
                <label>Title: </label> {{variant.title}}
            </li>
            <li>
                <label>Price: </label> ${{variant.price}}
            </li>
            <li>
                <label>Product: </label> {{product.title}}
            </li>
            <li>
                <label>Available Inventory:</label> {{variant.inventory}}
            </li>
            <li>
                <label>Description:</label>
                {{variant.description}}
            </li>
        </ul>
        <p>
            <a href="/variants/{{variant.id}}/edit">Edit</a> | <a href="/variants/{{variant.id}}/delete">Delete</a>
        </p>
    {% endblock %}